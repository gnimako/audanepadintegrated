@model EmployeeViewModel
@{
    ViewBag.Title = "AUDA-NEPAD Integrated System";
    Layout = "_AdminLayout";
}
<div class="row">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-header" style="background-color:#d1e0e6">
                <h4 class="m-b-0" style="color:#1a262b">Manage Activity Type Look Up</h4>
            </div>



            <div class="card-body">
                <form asp-controller="admin" asp-action="editprofile" enctype="multipart/form-data" method="post">
                    <input hidden asp-for="Id" class="k-textbox" style="width:100%" value="@Model.Id">
                    <input hidden asp-for="IdentityUserId" class="k-textbox" style="width:100%" value="@Model.IdentityUserId">
                    <input hidden asp-for="ExistingPhotoPath" />

                    <div class="form-body">

                        <div class="row">
                            <div class="col-md-12 ">
                                <div class="form-group">

                                    <a class="k-button" asp-action="index" asp-controller="admin" id="btnAdd">
                                        <span class="k-icon k-i-plus-outline"></span> Add Activity Type
                                    </a>
                                    <br />
                                    @(Html.Kendo().Grid<LookUpTablesViewModel>()
                                                    .Name("grid")
                                                    .Columns(columns =>
                                                    {
                                                        columns.Bound(c => c.LookUp_Id).Hidden();
                                                        columns.Bound(c => c.LookUp_Name).Title("Activity Name");
                                                        columns.Bound(c => c.LookUp_Status).Title("Activity Status");
                                                        columns.Bound(c => c.TransactionDate).Title("Transaction Date").Format("{0: dd-MM-yyyy}");

                                                        //columns.Command(command => command.Custom("Edit Activity").Click("resetpass1")).Width(200);
                                                        columns.Command(command => command.Custom(" Edit").IconClass("k-icon k-i-track-changes").Click("resetpass1"));
                                                        columns.Command(command => command.Custom(" Delete").IconClass("k-icon k-i-delete").Click("resetpass1"));
                                                    })
                                                    .HtmlAttributes(new { style = "height: 550px;" })
                                                    .Scrollable()
                                                    .Groupable()
                                                    .Sortable()
                                                    .Navigatable()
                                                    .Filterable()
                                                    .Pageable(pageable => pageable
                                                        .Refresh(true)
                                                        .PageSizes(true)
                                                        .ButtonCount(5))
                                                    .DataSource(dataSource => dataSource
                                                        .Ajax()
                                                        .Read(read => read
                                                                        .Action("LkUp_ActivityType_Read", "Admin"))
                                                        .Model(model => model.Id(p => p.LookUp_Id))
                                                        .PageSize(20)
                                                    )
                                    )

                                </div>
                            </div>
                        </div>
                    </div>

                </form>
            </div>
        </div>
    </div>
</div>


<div id="getstaffwindow">
</div>
<div id="windowstaff"></div>
<div id="staffposwindow"></div>


<div id="windowstaff2"></div>
<div id="staffposwindow2"></div>

<div id="windowstaffpos">
</div>

<div id="example">
    <div id="dialog">
    </div>
    <div id="undo">
    </div>
</div>



<script type="text/javascript">

function resetpass1(e) {

        e.preventDefault();
        var dataItem = this.dataItem($(e.currentTarget).closest("tr"));

        var employeeid = dataItem.Programme_Id;


        var dialog = $('#dialog');
        var undo = $("#undo");






        function onClose() {
            undo.fadeIn();
        }




        $("#windowstaff2").append("<div id='getstaffwindow2'></div>");


        var wnd3 = $("#getstaffwindow2").kendoWindow({
            //actions: ["Close"],
            draggable: true,
            modal: true,
            animation: false,
            resizable: true,
            content: "/Admin/LoginPasswordReset?employeeid=" + employeeid,
            width: 800,
            height: 370,

            title: "Reset Password",
            // close: onClose,
            deactivate: function () {
                this.destroy();
            }

        }).data("kendoWindow");
        wnd3.center().open();





    }







function onClick() {

    }


    </script>

<script type="text/javascript">$("#btnAdd").click(function (e) {
        e.preventDefault();


        $("#windowstaff").append("<div id='getstaffwindow'></div>");


        var wnd2 = $("#getstaffwindow").kendoWindow({
            //actions: ["Close"],
            draggable: true,
            modal: true,
            animation: false,
            resizable: true,
            content: "/Admin/AddLoginAccess",
            width: 800,
            height: 400,

            title: "Add or Assign Login Access",
            // close: onClose,
            deactivate: function () {
                this.destroy();
            }

        }).data("kendoWindow");
        wnd2.center().open();

    });</script>





<style>
    /* Page Template for the exported PDF */
    .page-template {
        font-family: "DejaVu Sans", "Arial", sans-serif;
        position: absolute;
        width: 100%;
        height: 100%;
        top: 0;
        left: 0;
    }

        .page-template .header {
            position: absolute;
            top: 30px;
            left: 30px;
            right: 30px;
            border-bottom: 1px solid #888;
            color: #888;
        }

        .page-template .footer {
            position: absolute;
            bottom: 30px;
            left: 30px;
            right: 30px;
            border-top: 1px solid #888;
            text-align: center;
            color: #888;
        }

        .page-template .watermark {
            font-weight: bold;
            font-size: 400%;
            text-align: center;
            margin-top: 30%;
            color: #aaaaaa;
            opacity: 0.1;
            transform: rotate(-35deg) scale(1.7, 1.5);
        }

    /* Content styling */
    .customer-photo {
        display: inline-block;
        width: 32px;
        height: 32px;
        border-radius: 50%;
        background-size: 32px 35px;
        background-position: center center;
        vertical-align: middle;
        line-height: 32px;
        box-shadow: inset 0 0 1px #999, inset 0 0 10px rgba(0,0,0,.2);
        margin-left: 5px;
    }

    .customer-name {
        display: inline-block;
        vertical-align: middle;
        line-height: 32px;
        padding-left: 3px;
    }
</style>